# name: Build and Deploy

# on:
#   push:
#     branches: [ "main" ]

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout Code
#       uses: actions/checkout@v4

#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}

#     - name: Build and push backend image
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         push: true
#         tags: ${{ secrets.DOCKERHUB_USERNAME }}/indian-alt-backend:latest
#         cache-from: type=gha
#         cache-to: type=inline

#   deploy:
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     steps:
#     - name: Deploy to Azure VM
#       uses: appleboy/ssh-action@master
#       with:
#         host: ${{ secrets.AZURE_VM_IP }}
#         username: azureuser
#         key: ${{ secrets.AZURE_VM_SSH_KEY }}
#         script: |
#           cd ~/indian-alt
#           # Pull latest repository changes (for docker-compose.yml and nginx.conf)
#           git pull origin main
          
#           # Pull latest docker images
#           docker compose pull backend
          
#           # Restart containers with the updated images and configs
#           docker compose up -d
          
#           # Clean up old images to save disk space
#           docker image prune -f
          
#           # Check health of the backend
#           docker compose ps backend | grep "running" || exit 1
