services:
  # T4 profile: llama.cpp serves Gemma 4 because vLLM's Gemma 4 attention
  # kernel requires more shared memory than Turing provides.
  vllm-text:
    image: ${VLLM_IMAGE:-ghcr.io/ggml-org/llama.cpp:server-cuda}
    container_name: vllm-text-t4
    restart: unless-stopped
    ipc: host
    shm_size: 8g
    ports:
      - "${VLLM_PORT:-8000}:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      HF_TOKEN: ${HF_TOKEN:-}
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
    volumes:
      - ${HF_CACHE_DIR:-/mnt/vllm-cache/huggingface}:/root/.cache/huggingface
    command:
      - -hf
      - ${LLAMA_MODEL_SOURCE:-google/gemma-4-12B-it-qat-q4_0-gguf:Q4_0}
      - --alias
      - ${VLLM_TEXT_MODEL:-gemma-4-12b-it-q4}
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --api-key
      - ${VLLM_API_KEY:-local-dev-key}
      - --ctx-size
      - ${LLAMA_CONTEXT_SIZE:-32768}
      - --parallel
      - ${LLAMA_PARALLEL_SEQUENCES:-1}
      - --n-gpu-layers
      - ${LLAMA_GPU_LAYERS:-99}
      - --batch-size
      - ${LLAMA_BATCH_SIZE:-2048}
      - --ubatch-size
      - ${LLAMA_UBATCH_SIZE:-512}
      - --cache-type-k
      - ${LLAMA_CACHE_TYPE_K:-q8_0}
      - --cache-type-v
      - ${LLAMA_CACHE_TYPE_V:-q8_0}
      - --no-webui

  tei-embedding:
    image: ${TEI_IMAGE:-ghcr.io/huggingface/text-embeddings-inference:turing-1.9.3}
    container_name: tei-embedding-t4
    restart: unless-stopped
    shm_size: 1g
    ports:
      - "${EMBEDDING_PORT:-8081}:80"
    volumes:
      - ${HF_CACHE_DIR:-/mnt/vllm-cache/huggingface}:/data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
    command:
      - --model-id
      - ${EMBEDDING_MODEL_ID:-Qwen/Qwen3-Embedding-0.6B}
      - --hostname
      - 0.0.0.0
      - --port
      - "80"
      - --dtype
      - ${TEI_DTYPE:-float16}
      - --max-batch-tokens
      - ${EMBEDDING_MAX_BATCH_TOKENS:-8192}
      - --auto-truncate
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:80/health', timeout=5).read()\""]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s

  tei-reranker:
    image: ${TEI_IMAGE:-ghcr.io/huggingface/text-embeddings-inference:turing-1.9.3}
    container_name: tei-reranker-t4
    restart: unless-stopped
    shm_size: 1g
    ports:
      - "${RERANKER_PORT:-8082}:80"
    volumes:
      - ${HF_CACHE_DIR:-/mnt/vllm-cache/huggingface}:/data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
    command:
      - --model-id
      - ${RERANKER_MODEL_ID:-BAAI/bge-reranker-base}
      - --hostname
      - 0.0.0.0
      - --port
      - "80"
      - --dtype
      - ${TEI_DTYPE:-float16}
      - --max-batch-tokens
      - ${RERANKER_MAX_BATCH_TOKENS:-2048}
      - --auto-truncate
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:80/health', timeout=5).read()\""]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s
